# GitHub repository metadata

Use the following repository settings.

## Description

> Native macOS quota monitor and hybrid safety guard for OpenAI Codex — alerts, task blocking, Accessibility soft-stop, emergency stop, Telegram, and menu bar.

## Topics

`codex`, `openai-codex`, `macos`, `swift`, `swiftui`, `menu-bar`, `rate-limits`, `accessibility`, `telegram`, `developer-tools`

## Recommended settings

- Enable Discussions after the first public release.
- Enable private vulnerability reporting.
- Require the `Core tests` and `macOS build` checks before merging.
- Prefer squash merge for feature pull requests.
- Delete head branches after merge.
- Enable branch protection for `main` after the bootstrap PR merges.
